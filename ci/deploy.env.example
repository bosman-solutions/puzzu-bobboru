# Copy to ci/deploy.env and fill in. ci/deploy.env is gitignored.

# cluster
NFS_SERVER=10.0.0.10
NFS_PATH=/srv/nfs/<app>/data
API_SERVER=https://10.0.0.10:6443
INGRESS_HOST=app.example.com

# github — fine-grained PAT, repo Administration: read+write
GITHUB_PAT=
